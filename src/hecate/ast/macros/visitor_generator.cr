module Hecate::AST
  module Macros
    # Visitor generation is handled directly in the DSL context where
    # the nodes are defined, since we need access to the compilation
    # context where the node definitions exist.
  end
end